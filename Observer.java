// interface Observer yang akan digunakan pada class customer
public interface Observer
{
    /**
     * method abstract yang akan diimplementasikan pada class object customer
     */
    public void update(String s);
}
